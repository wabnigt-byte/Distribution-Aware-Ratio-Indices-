# Distribution-Aware-Ratio-Indices-
 A small collection of experimental ratio indices for R, where the core math is fixed but the output is reshaped to a chosen probability mass function (PMF). Not arbitrary sample distribution!
